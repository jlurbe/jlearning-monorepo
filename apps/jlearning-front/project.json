{
  "name": "jlearning-front",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/jlearning-front/src",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project jlearning-front --web",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/jlearning-front"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "jlearning-front:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "jlearning-front:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "jlearning-front:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "jlearning-front:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "jlearning-front:build:development"
        },
        "production": {
          "buildTarget": "jlearning-front:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/apps/jlearning-front"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
